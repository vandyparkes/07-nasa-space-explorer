// NASA APOD key. DEMO_KEY works on GitHub Pages with low rate limits (see https://api.nasa.gov/).
// For higher limits, request a key and replace the value below (do not commit private keys to a public repo).
const NASA_API_KEY = 'DEMO_KEY';
